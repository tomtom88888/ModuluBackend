player:
    send to player
    {"state": "prep_round"} when: waiting for host do: change to waiting for equation

    send to player
    {"state": "round_started"} when: round_starts do: change to send answer screen

    send to player
    {"state": "round_ended"} when: round_ends do: change to waiting for equation screen

    send to player
    {"state": "game_ended"} when: game_ends do: change to final score screen

    send to player
    {"state": "game_started"} when: game_starts do: change to waiting for equation screen

    recive from player
    {"state": answer, "time_took": time_took} when: player_sends_answer do: store answer

host: 
    send to host
    {"state": "prep_round", "equation": "equation"} when: round_starts do: a timer for equation display

    send to host
    {"state": "round_ended", "stats": {"player": score, "player": score}} when: round_ends do: display round scores

    recive from host
    {"state": "start_round"} when: host displayed equation do: send start to players

content: "arithmetic: (easy, medium, hard), algebra(easy, medium, hard)"